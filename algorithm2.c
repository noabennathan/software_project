#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "graph.c"
#include "linked_list.c"

Node** divide_a_group_into_two(Node*);
Node** divide_a_group_into_two(Node* group){

	/*this algorithm receives 2 nodes that their field "data" is the groups of *
	 * the nodes need to be divided *
	 * returns a pointer to an array of 2 nodes that contains the new 2 groups*/

}
